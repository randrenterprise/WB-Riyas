name: JavaGoat-WhatsApp-Bot
on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm start
        env:
          FIREBASE_URL: ${{ secrets.FIREBASE_URL }}
